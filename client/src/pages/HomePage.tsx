import React from 'react';

export default function HomePage() {
  return (
    <section>
      <h1>Studies</h1>
      <p>
        Study list and detail pages land in the next phase. For now, this page
        serves as the landing route so routing + layout are wired end-to-end.
      </p>
    </section>
  );
}
